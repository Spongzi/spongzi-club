spring:
  application:
    name: spongzi-club-oss
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 192.168.128.128:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        file-extension: yaml
#spring:
#  application:
#    name: jc-club-oss
#  profiles:
#    active: dev
#  cloud:
#    nacos:
#      config:
#        server-addr: 117.72.14.166:8848
#        prefix: ${spring.application.name}
#        group: DEFAULT_GROUP
#        namespace:
#        file-extension: yaml
